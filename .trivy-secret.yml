# Trivy secret scanning configuration
# Add allow-rules entries to suppress known false positives

allow-rules: []
  # Example suppression:
  # - id: aws-access-key-id
  #   paths:
  #     - Tests/Fixtures/

skip-dirs:
  - .git
  - DerivedData
  - .build

# Disable specific rules only with a documented reason:
# disabled-rules:
#   - rule-id
